### ScreenAutomation.ahk
- Need to make improvements around the user confirmation
    - Sometimes hidden window causes issues with other GUIs (deattivating elements, loosing spot on dropdowns, etc)
    - User confoirmation window default position needs to be consomizable based on user preference via saved file
- Update ReadMe with details on each function and review for removing BETA status

### _SnippetManager
- Allow inclduing typical modifier keys as a keybind (ex. RShift)